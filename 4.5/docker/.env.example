# =============================================================================
# Environment Variables for Docker Compose
# =============================================================================
# Copy this file to .env and fill in the actual values
# DO NOT commit .env with real secrets!

# Grafana Admin Password
# Used by: grafana service in docker-compose.yml
# Security: MUST be a strong password (min 16 chars, alphanumeric + symbols)
GRAFANA_ADMIN_PASSWORD=your_secure_grafana_password_here

# Database Password (if needed for future services)
DATABASE_PASSWORD=your_secure_db_password_here

# Backup GPG Passphrase (for encrypted backups)
BACKUP_GPG_PASSPHRASE=your_secure_gpg_passphrase_here

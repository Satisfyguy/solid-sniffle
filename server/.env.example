# Monero Marketplace Server - Environment Configuration
# Copy this file to .env and configure for your environment

# Database Configuration
# SQLite database file path (will be encrypted with SQLCipher)
DATABASE_URL=marketplace.db

# Database Encryption Key (SQLCipher)
# CRITICAL: This key is used to encrypt the entire database at rest
# - Generate with: openssl rand -hex 32
# - Store securely, NEVER commit to version control
# - Loss of this key = permanent data loss
# - For development/testing only, use a non-empty string
DB_ENCRYPTION_KEY=dev_encryption_key_change_me_in_production

# Logging Configuration
# Valid levels: trace, debug, info, warn, error
RUST_LOG=info,diesel=warn,actix_web=info

# Monero RPC Configuration (from monero_marketplace_common)
# MONERO_RPC_URL is configured in common crate (default: http://127.0.0.1:18082)

# Server Configuration
# SERVER_BIND_ADDR=127.0.0.1
# SERVER_PORT=8080

# Security Configuration
# WARNING: In production, use a key management system
# For development, encryption key is generated randomly on startup (ephemeral)
# ENCRYPTION_KEY_FILE=/path/to/secure/key/file

# Tor Configuration (for future use)
# TOR_SOCKS_PROXY=socks5h://127.0.0.1:9050
